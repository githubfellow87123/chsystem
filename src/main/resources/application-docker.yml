spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/chsystem
    driverClassName: org.postgresql.Driver
    username: chsystem
    password: chsystem
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
